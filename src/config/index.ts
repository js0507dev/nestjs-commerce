export { commonConfig, CommonConfig } from './common.config';
export { databaseConfig, DatabaseConfig } from './database.config';

export enum ConfigToken {
  Common = 'common',
  Database = 'database',
}
